# Base URL API cho FE gọi.
# BE local hiện không set global prefix, nên /auth/me => http://localhost:8000/auth/me.
# Prod: đổi thành domain API thật nếu deploy sau này.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
