-- Bỏ hẳn luồng mời qua email: chỉ còn vào tổ chức bằng mã tham gia (gõ tay hoặc qua link mời).
-- Xoá bảng kéo theo mọi index và khoá ngoại của nó, không cần lệnh drop riêng.
DROP TABLE "organization_invitations";
